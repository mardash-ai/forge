node_modules/
dist/
*.log

# Forge local workspace + state (generated apps and Resource/Event store)
/workspace/
.forge/

# Internal, not for publication
docs/SETUP_MARDASH_AI.md
forge-agent-context.md
FORGE_SAMPLE_PROJECT_IDEAS.md
# terraform working dirs — `terraform init` drops 100MB+ provider binaries here
terraform/**/.terraform/
terraform/**/.terraform.lock.hcl

# console SPA build output + deps
console/dist/
console/node_modules/

# tsc -b emits alongside the sources; only the Vite bundle ships
console/src/**/*.js
console/src/*.js
console/tsconfig.tsbuildinfo
