# 3 · Runtime call paths — retired

> **This page has been retired.** The per-capability HTTP endpoint inventory (routes, request/response
> shapes, and error codes) is now generated from the live capability registry and available at
> [Platform model](PLATFORM_MODEL.md). For adoption patterns — how the consumer app reaches each
> capability, the two-token auth design (short-lived JWS + opaque refresh, same-origin proxy, mirrored
> session contract), and per-capability client conventions (fire-and-forget writes, owner scoping,
> detectable degradation) — see [05 · Adoption model](05-adoption-model.md).
